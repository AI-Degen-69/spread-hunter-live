# Issue #195 Tasks

- [x] T1: Add `MAX_BUY_SLIPPAGE` and `_buy_from_asks` in `core_brain/shadow_exec.py`.
- [x] T2: Wire walked BUY size and price into completion persistence and response in `core_brain/shadow_exec.py`.
- [x] T3: Add the thin-ask, short-fill, weighted-price regression test in `tests/test_exit_price_realism.py`.
- [x] T4: Run `python -m pytest -q` and review the resulting diff.
